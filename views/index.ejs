<h1>All blogs</h1>

<% blogs.forEach((blog)=>{ %>
<div class="">
    <p><%= blog.title %></p>
    <p><img src="<%= blog.image %>"></p>
    <p><%= blog.body %></p>
    <p><%= blog.date %></p>
</div>
<% }) %>